[tox]
envlist = py3
isolated_build = True

[tool:pytest]
addopts = --doctest-modules

[testenv]
deps =
    pytest
    pytest-cov
extras = all
commands =
    pytest tests {posargs}

[testenv:test]
extras = test
commands =
    pytest tests/test_import.py {posargs}
